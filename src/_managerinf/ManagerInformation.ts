import VersionNumber from 'src/model/VersionNumber';

export default class ManagerInformation {
    public static VERSION: VersionNumber = new VersionNumber('3.0.32');
    public static IS_PORTABLE: boolean = false;
}
